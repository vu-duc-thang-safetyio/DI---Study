# DI---Study
A study on dependency injection

## Workspace

- `packages/app`: a minimal TypeScript app package with `index.ts`
- `packages/framework`: the TypeScript library package with a barrel `index.ts`, `injector.ts`, and `engine.ts`
